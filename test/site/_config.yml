name: Test site
gems:
  - octopress-paginate
timezone: UTC
markdown: redcarpet

pagination:
  posts:
    per_page: 2 # used to be 'paginate'
    template: /blog/index.html # new! specify where we should look for your template
    permalink: /blog/:num # used to be 'paginate_path'
    limit: 3 # number of paginated pages
    filter_by:
      category: blog
